using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PuzzleTwin : MonoBehaviour
{
    //as you can see, this script is empty, this is a suple nod to the fact that I dont know what I'm doing


    // Start is called before the first frame update
    void Start()
    {
        
    } 

    // Update is called once per frame
    void Update()
    {
        
    }
}
